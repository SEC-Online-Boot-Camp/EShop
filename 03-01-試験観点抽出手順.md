# 03-01 試験観点抽出手順

## 目的

クーポン機能のAPI設計書と、実装済みの配布物をもとに、AIで試験観点を網羅的に洗い出す。

## 前提条件

- No.1でクローンした`EShop`のフォルダと仮想環境（`.venv`）がそのまま残っている。
- No.2で作った`docs/要件整理メモ.md`・`docs/クーポンAPI設計書.md`が`EShop`の中にある。
- GitHubの`EShop`リポジトリに接続でき、`No3`ブランチを取得できる。
- Claude CodeとVS Code拡張が使える状態になっている（01-02・01-03を完了している）。

No.3は**No.1・No.2で使ってきた`EShop`をそのまま使う**。新しくクローンし直す必要はない。
クーポン機能を実装済みにしたコードは`No3`ブランチにあり、ブランチを切り替えるだけでその状態になる。

No.3では**クーポン機能の実装は行わない**。すでに実装されているコードに対して試験をするのがこの回の課題である。

> 追加されるコードには**意図的な誤りが含まれている**。「実装済み＝正しく動く」ではないので、
> 設計書どおりに動くかを自分の観点で確かめること。何件あるかは伏せてある。

## 手順

### 0. No3ブランチに切り替える

`EShop`フォルダを開いた状態のターミナルで、`No3`ブランチに切り替える。

```powershell
git fetch origin
git switch No3
```

変わるのは10ファイルだけで、`.env`・`CLAUDE.md`・`docs/`・`.venv`など**自分が手を入れたものはそのまま残る**。
No.2で作った設計書を移す作業は不要である。

```powershell
git branch --show-current     # No3と表示されることを確認
Get-ChildItem app\coupon.py   # 追加されたことを確認
Get-ChildItem docs            # No.2の成果物が残っていることを確認
```

> 切り替えのときに`M .env`・`M .gitignore`と表示されるのは、No.1で自分が変更した内容が
> そのまま引き継がれたという意味である。エラーではないのでそのまま進めてよい。

### 0-2. データベースを作り直し、テストが通ることを確認する

仮想環境を有効化し、データベースを作り直してクーポンのシードデータを入れる。

```powershell
.venv\Scripts\activate
Remove-Item ecommerce.db -ErrorAction SilentlyContinue
python -m app.seed
pytest
```

**`ecommerce.db`の削除は必須である。** クーポン機能では`orders`テーブルに`coupon_code`・
`discount_amount`の2列が増えるが、`seed`が使う`create_all()`は既存テーブルに列を追加しない。
No.1で作ったDBファイルをそのまま使うと、注文確定で
`table orders has no column named coupon_code`というエラーになる。
DBの中身はすべて`seed`で作り直せるので消して問題ない（No.1・No.2の成果物は
`.env`・`CLAUDE.md`・`docs/`にあり、DBには入っていない）。

`python -m app.seed`は「ユーザー2件・商品5件・クーポン6件を投入しました。」と表示し、
ユーザー2件・商品5件・クーポン6件を投入する。`pytest`は**55件PASS**する。
これが**回帰試験の基準線**であり、No.3の最後にもこれが全件PASSしている必要がある。

> `pytest`はテスト用の別DBを使うため、`ecommerce.db`を消し忘れていても55件PASSしてしまう。
> 消し忘れたまま進めると、Swagger UIで注文確定を試したときに初めて気づくことになる。
>
> `pytest`がここで失敗する場合は、ブランチの切り替えが正しくできていないか、No.1・No.2で`EShop`の
> コードを変更している可能性がある。先に講師に申し出ること。

### 1. 実装済みのAPIを確認する

`uvicorn app.main:app --reload`を起動し、Swagger UI（`http://127.0.0.1:8000/docs`）で
クーポン関連のエンドポイントを確認する。

| メソッド | エンドポイント | 概要                                     | 成功時 |
| :------- | :------------- | :--------------------------------------- | :----- |
| POST     | /cart/coupon   | クーポン適用（プレビュー。消費はしない） | 200    |
| DELETE   | /cart/coupon   | 適用中のクーポンを取り消す               | 200    |
| GET      | /cart          | カート内容・小計・割引後金額取得         | 200    |
| POST     | /orders        | 注文確定（クーポンの再検証・消費を含む） | 201    |

`POST /orders`だけが201である。設計書に書き忘れやすい項目なので、Swagger UIで実際の値を
確かめて、次の手順の前提に反映すること。ここを間違えると、03-02で生成したテストが
**正常系すべてステータスコードの相違だけで失敗する**。

`README.md`（`No3`ブランチで内容が追加されている）の「No.3 試験・品質管理版」以降に、投入済みのクーポン一覧（有効・期限切れ・上限到達・上限なし）が載っている。
どんなデータで試験できるかを先に把握しておく。

### 2. 前提を決めて、AIに試験観点の抽出を依頼する

02-02で作った設計書と、配布物の実装は**一致しているとは限らない**。
たとえばクーポン適用を`/cart`の拡張として設計した場合、配布物は新規エンドポイント`POST /cart/coupon`に
なっている。まずどちらの仕様で試験するかを決める。

配布物の実装に対して試験するので、**配布物の仕様を基準**とし、自分の設計との差分は
「なぜその設計にしたか」を振り返る材料として残しておく。

設計書だけでなく**02-01の要件整理メモも渡す**。設計書に落ちていない判断（有効期間の境界を
どちらに含めるか、端数をどう処理するかなど）がメモ側にだけ残っていることがあり、
渡さないと「設計書に記載なし」として暫定値を置く羽目になる。

決めた前提は、観点抽出の依頼と**同じプロンプトに書いて渡す**。前提だけを先に送ると、
AIはそれを依頼と受け取って観点まで出力してしまい、条件を後から足す形になる。
設計書は`@`で指定する。仕様を要約して渡すと、要約の時点で落とした条件がそのまま観点の抜けになる。

> `@docs/クーポンAPI設計書.md`のように書くと、Claude Codeがその場でファイルを読み込む（02-02と同じ）。

```text
あなたはソフトウェアテストエンジニアです。
以下のAPI設計書をもとに、試験観点を正常系・異常系・境界値に分類して網羅的に列挙してください。
観点ごとに期待結果（HTTPステータスと金額）も書いてください。
金額はREADMEの投入済みクーポンの実データを使い、具体的な数値まで書いてください。

【前提】
- 試験対象は POST /cart/coupon（プレビュー）と POST /orders（確定）です
- 複数クーポンの併用は今回のスコープ外（1注文につき1枚）です
- 試験観点は設計書を根拠にしてください。ロジック（app/coupon.py・app/routers/配下）は読まないでください
- データ定義（app/models.py・app/schemas.py）を参照し、項目名と型を配布物に合わせてください。
  設計書にあって配布物のデータ定義に存在しない項目は、試験できないので観点から外してください
- 成功時のHTTPステータスは POST /orders が 201、それ以外は 200 です
- エラーレスポンスの detail の文言は検証対象外です。異常系はHTTPステータスコードで判定してください
- 試験に使えるクーポンと商品のデータは、README の「No.3 試験・品質管理版」以降に記載のものです

【API設計書】
@docs/クーポンAPI設計書.md

【要件整理メモ】
@docs/要件整理メモ.md

【README（API一覧・投入済みクーポン）】
@README.md
```

> 併用可否を「可」で設計した場合もある。その場合も、**配布物は併用不可で実装されている**ため、
> 試験の前提は併用不可に揃える。設計と実装が食い違ったときに試験側をどちらに合わせるかという
> 判断自体が、実務でよくある論点である。
>
> **実装のロジックを読ませないことが重要である。** Claude Codeはワークスペース内のファイルを
> 自分で読めるため、指示しないと`app/coupon.py`などを読み、実装のとおりの観点を出してくる。
> それでは「設計書どおりに動くか」を確かめる試験にならず、実装に仕込まれた誤りもそのまま追認される。
> `app/models.py`と`app/schemas.py`を例外にしているのは、設計書と実装で項目名やカラムの有無が
> 食い違っていると、03-02で生成したテストが項目名の違いだけで失敗し、実装の誤りと区別できなく
> なるためである。設計書にしかない項目（`is_active`など）でテストを書かせないためにも必要になる。
> どちらもカラムと型の定義だけでロジックを含まないため、仕込まれた誤りは漏れない。
> 実装を根拠にした観点が混ざっていたら、設計書のどこに書かれているかを聞き返して確かめること。

### 3. 抜け漏れを補完する

最初の出力で終わらせず、以下を**1問ずつ**追加で問いかける。回答ごとに、追加された観点が
妥当かを自分で確かめてから次に進む。まとめて渡すと手順2の依頼を大きくしただけになり、
1問あたりの回答が浅くなる。最後の問いは、前の問いの結果を踏まえて聞くことで効いてくる。

```text
最低購入金額の判定について、「ちょうど」の場合はどちらに倒れますか？
その観点はシートに入っていますか？
```

```text
割引額に1円未満の端数が出るのはどういうときですか？その観点を追加してください。
端数は、切り捨てと四捨五入で結果が変わる金額（小数部が0.5より大きいもの）を選んでください。
```

```text
発行数上限（usage_limit）が設定されていないクーポンの観点はありますか？
```

```text
セール品や対象外カテゴリを含むカートに適用した場合の観点を追加してください。
```

```text
他に見落としている境界値のケースはありますか？
```

```text
期待結果が「未定義」「要確認」のままになっている観点はありますか？
設計書に定義がないものは暫定の期待値を決め、「暫定」と判断の根拠を明記してください。
```

> 1つ目と3つ目がこの手順の山場である。
> 「〜以上」という条件は、実装では`<`か`<=`のどちらかになる。**境界そのものを疑う**観点が
> ないと、1円ずれた実装をいつまでも検出できない。
>
> 「上限が設定されていない」も同じ構造である。値が入っているケースだけを試験して、
> **未設定（NULL）のケースを試験しない**のはよくある抜けである。
>
> 2つ目で**金額の条件まで指定している**のには理由がある。端数が0.5以下だと切り捨てても
> 四捨五入しても同じ値になるため、丸め方式が違っていてもテストがPASSしてしまう。
> 「端数が出る金額」と言うだけでは`1,333円×10%＝133.3`のような例が返ってきて、
> 観点はあるのに欠陥を検出できないシートになる。`3,705円×15%＝555.75`のように、
> **丸め方式で答えが変わる金額**を選ばせること。
>
> 最後の問いかけは、**自分の設計書の穴を回収するため**のものである。設計書に書かれていない
> 条件について、AIは正直に「未定義」「要確認」と返してくる。それ自体は正しい挙動だが、
> 期待結果が空のままではテストコードにできない。実務では**試験担当が暫定の期待値を決めて
> 明記し、発注元に確認を出す**。02-02の設計が甘いほどここで未定義が増える。
> 上流の質が下流に伝播することを、自分の成果物で確認する場面である。
>
> 時間が足りない場合は、**2つ目（端数）と3つ目（`usage_limit`未設定）だけは必ず単独で問いかけ**、
> 残りをまとめて渡してもよい。この2問だけが、値が入っているケースを試験しているだけでは
> 絶対に見つからない種類の抜けを埋めるものである。

### 4. 回帰試験の観点を追加する

ここまでの観点は、すべて「新しく足したクーポン機能」に対するものである。
既存の機能を壊していないかを確認する観点は、AIに聞かないと出てこない。

```text
このクーポン機能の追加によって、既存の /cart・/orders の
クーポンを使わない通常フローが壊れていないかを確認する観点を追加してください。
```

最低限、以下はシートに入れること。

- クーポンなしで注文を確定できる（`coupon_code`はnull、割引額は0）
- クーポン未適用の`GET /cart`で割引額が0、`total`が`subtotal`と一致する
- 既存テスト55件が全件PASSする

### 5. 試験観点シートに統合する

ここまでで観点は、手順2の初回出力と手順3・手順4の問いかけの回答に分散している。
これを1枚のシートに統合する。**書き写す作業はAIにやらせて、自分はレビューに回る。**

```text
ここまでに洗い出した観点を、1つの試験観点シートに統合してください。

【形式】
- 区分（正常系／異常系／境界値／回帰）ごとの表にまとめ、No.を振り直す
- 列は「No.・試験観点・前提条件・期待ステータス・期待金額」
- 期待金額は具体的な数値まで書く。「正しく計算される」のような書き方はしない
- 自動テストにしづらい観点（並行実行など）は期待結果に「手動確認」と書く
- 暫定で決めた期待値は「暫定」と根拠を残す

【あわせて直してほしい点】
- 確認したい条件以外が境界と重なっている観点は、データを境界から離してください
- 重複している観点は統合してください
```

出力されたシートは必ず自分で確認する。以下は**AIに任せず人間が見る**項目である。

| 確認項目             | 見るポイント                                      |
| :------------------- | :------------------------------------------------ |
| 4区分が揃っているか  | 特に**回帰**が落ちていないか                      |
| 期待金額が数値か     | 式や「正しく計算される」で止まっていないか        |
| 観点が減っていないか | 手順3・手順4で追加した観点が全部残っているか      |
| 暫定値の行方         | 6問目で暫定値を決めた項目が観点として残っているか |
| 境界の重複           | 1つの観点が複数の境界を同時に踏んでいないか       |
| 「未定義」の残り     | 暫定値か「手動確認」のどちらかに寄せられているか  |

> **統合でこそ観点が落ちる。** AIは長い会話をまとめるとき、後半で追加した観点や
> 例外的なケースを省きやすい。手順2の初回出力の件数と、手順3・手順4で追加した件数を
> 控えておき、統合後のシートと数を突き合わせること。
>
> 特に落ちやすいのが、**手順3の6問目で暫定値だけ決めて観点表に書いていなかった項目**である。
> 件数の突き合わせでは気づけないので、6問目の回答を見ながら1件ずつ確認すること。
> 「テストデータを組み直す必要がある」と指摘した観点が、組み直されずに削除されていないかも見る。
>
> 期待結果の欄に「手動確認」と書いた観点はシートから消さずに残す。
> 03-02でテストコードにするのは自動化できるものだけでよい。

### 6. 試験観点シートを保存する

No.2から使っている`docs`フォルダに、`docs/試験観点シート.md`として保存する。

このシートは03-02（テストコード生成）のインプットになるため、講座終了後も残しておくこと。

## 完了条件

- 試験観点シートに正常系・異常系・境界値・**回帰**の4区分が揃っている
- 各観点の期待結果に、HTTPステータスだけでなく**具体的な金額**が書かれている
- 期待結果が「未定義」「要確認」のまま残っている観点がない（暫定値と根拠、または「手動確認」が書かれている）
- 最低購入金額ちょうど・端数・割引上限・発行数上限なし・セール品混在の観点が含まれている
- 試験観点シートが`docs/試験観点シート.md`として保存されている

以上
