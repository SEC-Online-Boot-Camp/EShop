---
marp: true
theme: slide
math: katex
header: 開発業務効率化実践講座（AI活用入門）<br> ソフトウェアエンジニア編
footer: Copyright © 2026 Systems Engineering Consultants Co.,LTD. All rights reserved. 無断転載・複写禁止
---

# AIを活用した試験・品質管理

---

## 目次

- 導入
- 試験観点抽出
- テストコード生成
- デバッグ
- レビュー・改善
- まとめ

---

## 1. 導入

---

### 試験工程でAIが担えること

| AI が得意なこと                | AI が苦手なこと                      |
| :----------------------------- | :----------------------------------- |
| 試験観点の網羅的な洗い出し     | ビジネス観点の優先度判断             |
| テストコードの叩き台生成       | 本番環境特有のケースの発見           |
| エラーの原因仮説と修正案の提示 | テスト実行・環境構築                 |
|                                | もっともらしい誤答・ハルシネーション |

**AIは「漏れを補い、叩き台を作る」パートナー**
テストの妥当性は人間が担保する

---

### 今日の題材：クーポン機能は実装済みです

No.1・No.2で使ってきた`EShop`で、**`No3`ブランチに切り替えて**使います。

- 新しいプロジェクトは作りません。No.2で作った`docs/`の成果物もそのまま使えます
- 今日は実装作業はしません。**試験する側**に回ります
- 追加されるAPI：`POST /cart/coupon`・`DELETE /cart/coupon`（`GET /cart`・`POST /orders`も拡張）
- 投入済みのクーポン：有効／期限切れ／上限到達／**上限なし** など6種類

> **このコードには意図的な誤りが含まれています。**
> 「実装済み＝正しく動く」ではありません。設計書どおりに動くかを、自分の試験で確かめてください。

---

### 今日の流れ

| ステップ         | やること                                     | 成果物           |
| :--------------- | :------------------------------------------- | :--------------- |
| 試験観点抽出     | 設計書からAIで観点を洗い出す                 | 試験観点シート   |
| テストコード生成 | 観点をpytestのコードに落とす                 | テストコード     |
| デバッグ         | 失敗したテストの原因を特定し、実装を修正する | デバッグレポート |

**設計書 → 観点 → コード → デバッグ**
上流の質が下流に伝播する。観点が甘ければテストも甘くなる

---

### 【デモ】試験観点の抽出の流れ

API設計書から試験観点を抽出する流れをお見せします

1. `docs/クーポンAPI設計書.md`をClaudeに渡す
2. AIが出力した観点シートをレビューし、問いかけで補完する
3. この後、同じ手順でハンズオンに入ります

> 講師の画面を見ながら、問いかけ方・レビューの仕方を確認してください

---

## 2. 試験観点抽出

---

### 試験観点の区分

| 区分   | 問いかけ                         | 例：POST /cart/coupon                    |
| :----- | :------------------------------- | :--------------------------------------- |
| 正常系 | 正しい入力で期待通りに動くか     | 有効なクーポンコードで割引後金額が正しい |
| 異常系 | 不正な入力でエラーが返るか       | 存在しないコード→404 / 上限超過→409      |
| 境界値 | 限界値・エッジケースで壊れないか | 最低購入金額ちょうど / 端数が出る金額    |
| 回帰   | 既存の機能を壊していないか       | クーポンなしの注文確定が今までどおり動く |

**3区分＋回帰。** 回帰はAIに聞かないと出てこない

---

### APIテストの典型観点パターン

- **認証・認可**：未認証アクセス / 権限のないユーザーのアクセス
- **入力バリデーション**：必須項目欠如 / 型不正 / 範囲外の値
- **正常系 CRUD**：作成・取得・更新・削除の各操作
- **エラーレスポンス**：ステータスコード・ボディのフォーマット
- **境界値**：最大文字数 / 日付の端値 / ゼロ / null / 空文字

> 実務ではセキュリティ・並行性・冪等性などこの枠に収まらない観点も重要です。
> AIに「他に見落としている観点はありますか？」と問いかけて補完する習慣をつけましょう。

---

### なぜ試験に漏れが生じるのか

- 実装者は「動く場合」を考えて作るため、異常系・境界値が抜けやすい
- レビューも同じ視点になりがちで、補完しにくい
- 新機能に注目が集まり、**既存機能への影響（回帰）**が後回しになる

**AIは実装者と異なる視点で観点を列挙できる**
→ 見落としやすい観点を補える

---

### 観点抽出のプロンプト例

```none
あなたはソフトウェアテストエンジニアです。
以下のAPI設計書をもとに、試験観点を
正常系・異常系・境界値に分類して網羅的に列挙してください。
期待結果（HTTPステータスと金額）はREADMEの実データで具体的に書いてください。

【前提】
- 試験対象は POST /cart/coupon と POST /orders
- 複数クーポンの併用はスコープ外（1注文につき1枚）
- ロジック（app/coupon.py・app/routers/）は読まない。データ定義は参照可

【設計書・要件整理メモ・README】
@docs/クーポンAPI設計書.md
@docs/要件整理メモ.md
@README.md
```

> **前提は依頼と同じプロンプトに書くこと。** 前提だけ先に送ると、AIは観点まで出してしまいます
>
> **実装のロジックを読ませないこと。** 実装を見て書いた観点は、実装の誤りをそのまま追認します

---

### Claude の出力例：試験観点シート

| 区分   | No. | 試験観点                             | 期待結果                     |
| :----- | :-: | :----------------------------------- | :--------------------------- |
| 正常系 |  1  | 有効な定率クーポンを適用する         | 200 / 割引400円・合計3,600円 |
| 正常系 |  2  | 有効な定額クーポンを適用する         | 200 / 割引500円・合計3,500円 |
| 異常系 |  3  | 存在しないクーポンコードを指定する   | 404 Not Found                |
| 異常系 |  4  | 最低購入金額未満で適用する           | 422 Unprocessable Entity     |
| 異常系 |  5  | 発行数上限に達したクーポンを適用する | 409 Conflict                 |
| 境界値 |  6  | 小計を上回る定額クーポンを適用する   | 200 / 合計0円（負にしない）  |
| 境界値 |  7  | 割引計算で1円未満の端数が発生する    | 200 / 切り捨てた金額         |

**期待結果に金額まで書く。**「正しく計算される」では検証できない

---

### 観点の抜け漏れを補完する

最初の出力で終わらず、追加で問いかけて観点を補完する

```none
「最低購入金額の判定について、"ちょうど"の場合はどちらに倒れますか？」

「発行数上限（usage_limit）が設定されていないクーポンの観点はありますか？」

「セール品や対象外カテゴリを含むカートに適用した場合の観点を追加してください」

「他に見落としている境界値のケースはありますか？」

「期待結果が"未定義"のままの観点はありますか？暫定値と根拠を明記してください」
```

> 「〜以上」という条件は実装では `<` か `<=` のどちらかになります。
> **境界そのものを疑う**観点がないと、1円ずれた実装をいつまでも検出できません
>
> 設計書に定義がない条件はAIが正直に「未定義」と返します。**暫定値を決めて明記する**のが実務です

---

### 忘れられがちな観点：回帰試験

新機能の観点だけ聞くと、**既存機能への影響は返ってきません**

```none
このクーポン機能の追加によって、既存の /cart・/orders の
クーポンを使わない通常フローが壊れていないかを確認する観点を追加してください。
```

最低限これはシートに入れる

- クーポンなしで注文を確定できる（`coupon_code`はnull、割引額は0）
- クーポン未適用の`GET /cart`で`total`＝`subtotal`
- 既存テスト55件が全件PASSする

---

### 【ハンズオン】試験観点の抽出

#### 目的

- API設計書と実装済みAPIをもとに試験観点を網羅的に洗い出す

#### 手順

- ハンズオン手順書「03-01-試験観点抽出手順.md」に従って操作します

#### 完了条件

- 正常系・異常系・境界値・回帰の4区分が揃い、期待結果に金額が書かれている
- 「未定義」のまま残った観点がない（暫定値と根拠、または「手動確認」を記載）
- `docs/試験観点シート.md`として保存されている

---

### 【演習】各自の業務で実践

#### 目的

- 自分の業務のAPIや機能について試験観点を洗い出す

#### 手順

1. 自分の業務から対象とするAPIや機能を1つ選ぶ
2. ハンズオンで使ったプロンプトの型でAIと対話しながら試験観点シートを作成する
3. 「他に見落としている観点はありますか？」「既存機能への影響は？」で補完する

#### 完了条件

- 自分の業務の1機能について正常系・異常系・境界値・回帰が揃っている

---

## 3. テストコード生成

---

### テストコード生成のプロンプト例

```none
あなたはPythonのバックエンド開発者です。
以下の試験観点をもとに、FastAPI + pytest のテストコードを作成してください。

【試験観点】
@docs/試験観点シート.md

【利用できるフィクスチャ（@tests/conftest.py に定義済み）】
client, auth_headers, db_session, sample_product, cart_with_items,
sale_product, sample_coupon, fixed_coupon, unlimited_coupon,
exhausted_coupon, expired_coupon, make_product(...), make_coupon(...)

【条件】
- 実装コード（app/配下）は読まず、試験観点とフィクスチャだけを根拠にすること
- ステータスコードだけでなく、金額（discount_amount, total）も検証すること
- 端数や境界値のテストは make_product / make_coupon でデータを組み立てること

【出力先】
tests/test_coupon.py として保存してください
```

---

### Claude の出力例：テストコード

```python
class TestApplyCouponSuccess:
    def test_apply_percentage_coupon(
        self, client, auth_headers, cart_with_items, sample_coupon
    ):
        """正常系：定率クーポンで割引後金額が返る"""
        res = client.post(
            "/cart/coupon",
            json={"coupon_code": sample_coupon.code},
            headers=auth_headers,
        )
        assert res.status_code == 200
        body = res.json()
        assert body["discount_amount"] == 400
        assert body["total"] == 3600
```

---

### テストコードの続き

```python
    def test_minimum_purchase_amount_exactly_met(
        self, client, auth_headers, sample_product, sample_coupon
    ):
        """境界値：最低購入金額ちょうど（3,000円）は適用できる"""
        client.post(
            "/cart/items",
            json={"product_id": sample_product.id, "quantity": 3},
            headers=auth_headers,
        )
        res = client.post(
            "/cart/coupon",
            json={"coupon_code": sample_coupon.code},
            headers=auth_headers,
        )
        assert res.status_code == 200
        assert res.json()["discount_amount"] == 300
```

---

### 境界値は「データを作れるか」で決まる

既製のフィクスチャは金額が割り切れる値になっている
→ **端数のテストは自分でデータを組み立てないと再現できない**

```python
def test_fraction_is_truncated(client, auth_headers, make_product, make_coupon):
    """境界値：3,705円の15%＝555.75円 → 切り捨てて555円"""
    product = make_product(price=1235)  # 1,235円 × 3点 = 3,705円
    coupon = make_coupon(value=15, min_purchase_amount=3000)
    ...
    assert res.json()["discount_amount"] == 555
```

> 「端数の観点」をシートに書くだけでは足りません。
> **端数が実際に発生するデータ**を作れて初めてテストになります

---

### テストコードのレビュー観点

- 試験観点シートの全項目がカバーされているか（**特に回帰**）
- assert の内容は適切か（**ステータスコードだけでなく金額も確認する**）
- 境界値のテストで、実際にその条件が成立するデータになっているか
- テスト間の依存関係はないか（独立して実行できるか）

> 金額が違うだけの不具合は**ステータスコード200のまま**返ってきます。
> `assert res.status_code == 200` だけのテストでは絶対に検出できません

---

### 【ハンズオン】テストコードの生成

#### 目的

- 試験観点シートをもとにテストコードを生成する

#### 手順

- ハンズオン手順書「03-02-テストコード生成手順.md」に従って操作します

#### 完了条件

- `tests/test_coupon.py` が収集エラーなく実行できる（テストの失敗はあってよい）
- 各テストが金額まで検証している

---

### 【演習】各自の業務で実践

#### 目的

- 自分の業務の試験観点シートをもとにテストコードの叩き台を生成する

#### 手順

1. 演習で作成した試験観点シートを使う
2. ハンズオンで使ったプロンプトの型でAIにテストコードを生成させる
3. レビュー観点に沿って出力を確認・修正する

#### 完了条件

- 自分の業務の1機能について、実行できるテストコードが完成している

---

## 4. デバッグ

---

### よくあるエラーシナリオ

発行数上限のない（`usage_limit`がNULLの）クーポンをテストしたところ…

```none
FAILED test_coupon.py::TestApplyCouponBoundary::test_unlimited_coupon_can_be_applied
AssertionError: assert 409 == 200
```

**期待**: 200 OK（無制限クーポンなので誰でも使える）
**実際**: 409 Conflict（発行数上限に達している扱い）

まだ誰も使っていないのに、なぜ上限到達になるのでしょうか？

---

### 原因の聞き方

```none
.venv\Scripts\pytest.exe --tb=no -q -rf
--disable-warnings を実行して、失敗しているテストを
原因ごとにグループ化してください。まだ修正はしないでください。

【設計書】
@docs/クーポンAPI設計書.md
```

> **テストの実行もコードの読み取りもAIができます。** 渡す必要があるのは、
> pytestの出力に現れない**設計書**だけです。仮想環境のパスは明示します
>
> **「直して」ではなく「原因を説明して」。** いきなり修正させると、AIはテスト側の
> 期待値を実装に合わせて書き換えて緑にすることがあります

---

### Claude の出力例：原因仮説

```none
原因の仮説：
usage_limit が NULL のとき、`or 0` によって右辺が 0 に置き換わります。
その結果 used_count が 0 でも「0 >= 0」が真となり、常に上限到達と
判定されます。「未設定なら 0」という置き換えが、意味としては
「上限 0 枚＝1枚も使えない」になってしまっています。

修正案：
None の判定を明示してください。

    if coupon.usage_limit is not None and coupon.used_count >= coupon.usage_limit:
```

**「修正して」ではなく「原因の仮説を」** と聞くと、検証できる答えが返る

---

### 1件直すと、次が失敗する

上記を直して再実行すると、今度は注文確定側が落ちます

```none
FAILED test_coupon.py::TestCheckoutWithCoupon::test_checkout_with_unlimited_coupon
AssertionError: assert 409 == 201
```

```sql
UPDATE coupons SET used_count = used_count + 1
WHERE code = :code AND used_count < usage_limit
```

SQLでは `x < NULL` は真でも偽でもなく **UNKNOWN** と評価され、行に一致しません。
→ 影響行数が0件になり、無制限クーポンが常に「上限到達」と判定される

**同じ原因（NULLの扱い漏れ）が2箇所にあった**

---

### 参考：この UPDATE 文の設計意図

```sql
UPDATE coupons
SET used_count = used_count + 1
WHERE code = :code
  AND (usage_limit IS NULL OR used_count < usage_limit);
```

- 判定と加算を**1文にまとめて**DB側で原子的に処理している
- SELECTで確認してからUPDATEする方式（Check-then-Act）は、確認と更新の間に
  別のリクエストが割り込み、上限を超えて適用され得るため使わない
- 排他制御としては正しく、**NULL比較だけが誤っていた**

> AIが出力するSQLにもNULL比較の見落としは紛れ込みます。そのまま採用せず検算しましょう

---

### デバッグで気をつけること

- エラーメッセージの**全文**を一括で渡す。一部だけでは原因特定が難しい
- 関連するコード（テスト関数・実装・**設計書の該当箇所**）も一緒に渡す
- 「修正してください」より「**原因の仮説を教えてください**」の方が検証しやすい
- AIの修正案は理由を理解してから適用する
- **1件直ったら全件回す。** 同じ原因が他の箇所にも潜んでいることがある

> **修正するのはテストではなく実装です。** テストの期待値を実装に合わせて
> 書き換えれば緑になりますが、それでは不具合が残ったままになります

---

### コードの脆弱性を自動チェックする：`/security-review`

デバッグで直した個々のバグとは別に、**コード全体の脆弱性**もAIでチェックできます。

- `/security-review` コマンドで、現在のブランチの変更をセキュリティ観点でレビューできる
- インジェクション・安全でないデシリアライゼーションなどの問題をPR到達前にキャッチできる
- 完全な代替にはならないため、人によるレビュー・CIでのスキャンと組み合わせる

---

### 【ハンズオン】デバッグ

#### 目的

- 失敗したテストの原因をAIと対話しながら特定し、実装を修正する

#### 手順

- ハンズオン手順書「03-03-デバッグ手順.md」に従って操作します

#### 完了条件

- 引数なしの`pytest`が全件PASSする（既存55件＋自分のクーポンテスト）
- `docs/デバッグレポート.md`が作成されている

---

### 【演習】各自の業務で実践

#### 目的

- 自分のテストコードを実行し、エラーをAIと対話しながら修正する

#### 手順

1. 演習で生成したテストコードを実行する
2. エラーメッセージと関連コードをまとめてAIに渡す
3. 仮説を検証しながら修正し、デバッグレポートを作成する

#### 完了条件

- テストが通過し、エラー・原因仮説・修正内容を記録したデバッグレポートが完成している

---

## 5. レビュー・改善

---

### グループワークの進め方（30分）

1. グループ分け（2〜3人）
2. **試験観点シート**を共有（1人 3〜5分）
3. 相互レビュー・フィードバック（1人 3〜5分）
4. フィードバックをもとに試験観点シートを改善（残り時間）

> テストコード・デバッグレポートのレビューは持ち帰り課題として各自で振り返ってください

---

### 相互レビューの観点

- 試験観点シートの抜け漏れはないか（正常系・異常系・境界値・**回帰**）
- 期待結果に具体的な金額が書かれているか
- テストコードが試験観点を正しくカバーしているか
- デバッグレポートに原因と修正の根拠が書かれているか
- 自分以外のエンジニアがレポートを読んで再現・修正できるか

---

## 6. まとめ

---

### 今日の成果物

| 成果物           | 保存先                     | 確認方法                                 |
| :--------------- | :------------------------- | :--------------------------------------- |
| 試験観点シート   | `docs/試験観点シート.md`   | 正常系・異常系・境界値・回帰が揃っている |
| テストコード     | `tests/test_coupon.py`     | `pytest` が全件PASSする                  |
| デバッグレポート | `docs/デバッグレポート.md` | エラー・原因仮説・修正の記録がある       |

---

### キーメッセージ

- 試験観点は**正常系・異常系・境界値＋回帰**で漏れを防ぐ
- 期待結果は**具体的な金額**まで書く。ステータスコードだけでは不具合を見逃す
- 境界値は「観点を書く」だけでなく、**その条件が成立するデータを作れて**初めてテストになる
- デバッグはエラーメッセージの**全文**と関連コード・設計書をまとめてAIに渡す
- 1件直ったら全件回す。**同じ原因が複数箇所に潜んでいる**ことがある
- 直すのは実装であってテストではない

---

### 2日間の振り返り

| No. | 学んだこと                       | 成果物                                           |
| :-: | :------------------------------- | :----------------------------------------------- |
|  1  | 環境構築・セキュリティ           | 開発環境 / チェックシート                        |
|  2  | プロンプト設計・API設計          | 要件整理メモ / API設計書                         |
|  3  | 試験観点・テストコード・デバッグ | 試験観点シート / テストコード / デバッグレポート |

---

### 次回予告：No.4 認定テスト

- No.1〜3 の学習内容を総合的に活用する
- 指定要件からAPI設計書・試験一式を作成する（個人作業）
- グループで成果発表・フィードバック

> 今日作った試験一式・デバッグレポートを振り返っておいてください
