# 02-02 API設計書作成手順

## 目的

要件整理メモと、EShopの既存API一覧をもとに、クーポン機能のAPI設計書の叩き台を作成する。

## 前提条件

- 02-01で要件整理メモ（`docs/要件整理メモ.md`）を作成している。
- Claude CodeとVS Code拡張が使える状態になっている（01-02・01-03を完了している）。
- EShopの`README.md`（既存API一覧を記載）を参照できる。

VS Codeで`EShop`フォルダを開き、右パネルのClaude Codeから対話する。この手順ではEShopのコードは変更しない（クーポン機能の実装はNo.3で行う）。

## 手順

### 1. 既存API一覧を確認する

EShopの`README.md`に記載されている既存APIを確認する。

| メソッド | エンドポイント | 概要                       | 認証 |
| :------- | :------------- | :------------------------- | :--- |
| GET      | /              | ヘルスチェック             | 不要 |
| POST     | /auth/login    | ログイン（JWT取得）        | 不要 |
| GET      | /products      | 商品一覧取得               | 不要 |
| POST     | /cart/items    | カートに商品追加           | 要   |
| GET      | /cart          | カート内容・小計取得       | 要   |
| POST     | /orders        | 注文確定（チェックアウト） | 要   |

`uvicorn`を起動してSwagger UI（`http://127.0.0.1:8000/docs`）を開くと、この一覧と同じエンドポイントが確認できる。

### 2. AIにAPI設計を依頼する

要件整理メモと既存API一覧をあわせてAIに渡し、クーポン機能のエンドポイントを設計させる。

Claude Codeでは、プロンプトの中に`@`付きでファイルパスを書くと、その場でファイルを読み込む。
内容をコピー＆ペーストする必要はない。要約して渡すと要約の時点で条件が落ちるため、
自分が作った成果物はこの方法でそのまま渡す。

> ヘルスチェック（`GET /`）はクーポン設計に関係しないため、プロンプトには含めていない。AIに渡すコンテキストは、判断に必要なものだけに絞る。

```text
あなたはバックエンドAPIの設計者です。
以下の要件整理メモと、既存のECサイトAPI一覧をもとに、
クーポン機能のエンドポイントをMarkdownのテーブル形式で設計してください。

【要件整理メモ】
@docs/要件整理メモ.md

【既存API】
POST /auth/login（認証不要）
GET /products（認証不要）
POST /cart/items（要認証）
GET /cart（要認証）
POST /orders（要認証）

【制約】
- 既存のAPIとの整合性を保つ（拡張するか新規エンドポイントにするかを判断する）
- RESTful設計に従う
- 認証が必要なエンドポイントには「要認証」と記載
```

### 3. 統合方法を検討する

AIの出力に対して、以下を確認・議論する。

- クーポンの適用・取消は新規エンドポイントにするか、既存の`/cart`に統合するか
- クーポン適用後の金額は`/cart`（プレビュー）と`/orders`（確定）のどちらで計算するか、両方で一致させるにはどうするか

```text
クーポン適用を新規エンドポイントにする案と、
既存の /cart を拡張する案のメリット・デメリットを比較してください。
```

比較したうえで**どちらの方式を採るかをここで決める**。決めた方式と理由は設計書に残すこと。次の手順4は、ここで決めたエンドポイントに対して行う。

決めた内容はAIにも伝える。AIは会話の前の方で出した論点を毎回覚えているとは限らないため、以降のプロンプトで前提がずれないよう、決定事項を明示的に共有しておく。

```text
（採用する方式）を採用することにしました。
理由：（決めた理由）
以降はこの方針を前提に進めてください。
```

続けて、2つ目の論点（プレビューと確定の金額をどう一致させるか）も同様に比較する。

```text
クーポン適用後の金額を、/cart（プレビュー）で計算した値をそのまま /orders（確定）
で使う案と、/orders 確定時に再計算・再検証する案を比較してください。
```

こちらも比較したうえで採用する案を決め、理由とあわせて設計書に残す。決めたらAIにも伝える。

```text
（採用する案）を採用することにしました。
理由：（決めた理由）
以降はこの方針を前提に進めてください。
```

### 4. リクエスト・レスポンス仕様を詰める

手順3で決めた方式のクーポン適用エンドポイントについて、リクエストボディ・レスポンスボディ・エラーレスポンスをAIと対話しながら決める。

以下は新規エンドポイント案を選んだ場合のプロンプト例である。`/cart`拡張案を選んだ場合は、エンドポイント名を自分が決めたもの（例：`PATCH /cart`）に読み替えること。

```text
POST /cart/coupon のリクエストボディとレスポンスボディを、
JSON形式の例で示してください。エラーレスポンス（404, 409, 422）も含めてください。
```

### 5. レビュー観点で確認する

- エンドポイントの粒度は適切か
- HTTPメソッドの使い分けは正しいか
- エラーレスポンスのステータスコードは定義されているか
- 要件整理メモの機能・条件・例外が設計に反映されているか
- 既存の`/cart`・`/orders`の正常系（クーポンなし）を壊していないか

### 6. API設計書を保存する

エンドポイント一覧とリクエスト・レスポンス仕様をまとめ、`docs/クーポンAPI設計書.md`として保存する。

この設計書は**No.3（試験・品質管理）のインプットになる**ため、講座終了後も残しておくこと。

## 完了条件

- クーポンの適用・取消エンドポイントと、既存`/cart`・`/orders`の拡張内容の叩き台が完成している
- 主要エンドポイント（クーポン適用）のリクエスト・レスポンス仕様が具体化されている
- API設計書が`docs/クーポンAPI設計書.md`として保存されている

以上
