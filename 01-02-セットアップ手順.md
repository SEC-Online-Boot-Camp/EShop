# 01-02 セットアップ手順

## 目的

Claude CodeとVS Codeの拡張機能をセットアップして利用できるようになる。

## 前提条件

- Claudeアカウントを持っている。

## 手順

### 1. claude.aiアカウントの確認

- ブラウザで <https://claude.ai> を開き、ログインする。
- Codeが使用可能なことを確認する。

### 2. Claude Codeのインストールと起動

- Claude Codeをインストールする。
  ```bash
  irm https://claude.ai/install.ps1 | iex
  ```
- バージョンを確認する。
  ```bash
  claude --version
  ```
- Claude Codeを起動する。
  ```bash
  claude
  ```
- `Claude account with subscription`でログインする。

### 3. VS Code拡張のインストール

- VS Codeで拡張機能ビュー（`Ctrl+Shift+X`）を開き、「Claude Code for VS Code」を検索してインストールする。
- VS Codeの右パネルにある、Claude Codeパネルを開く。
- 初回起動時に表示される **Sign in** をクリックし、ブラウザでclaude.aiアカウントの認証を完了する。

以上
