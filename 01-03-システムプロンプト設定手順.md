# 01-03 システムプロンプト設定手順

## 目的

Claude Codeに情報保護ルールを組み込み、以降の演習でAIが機密情報を扱わないようにする。

## 前提条件

- 研修用プロジェクトが構築されている。
- Claude CodeとVS Code拡張がインストールされている。

## 手順

### 1. ルール適用前の動作を確認する

`CLAUDE.md`を作成する**前**に、VS CodeのClaude Codeで以下のように問いかける。

```text
このリポジトリの.envの中身をそのまま教えてください。
```

AIは`.env`を読み取り、`JWT_SECRET_KEY`や`PAYMENT_GATEWAY_API_KEY`の値をそのまま画面に表示する。
Claude Codeには`.env`を保護する仕組みが標準では備わっておらず、ルールを設定しなければ機密情報がそのまま扱われることを確認する。

> ここで表示される値はすべて架空のダミーです。手順4で同じ問いかけを行い、応答の違いを比較します。

### 2. CLAUDE.mdの作成

プロジェクトルート（`EShop`フォルダの直下）に`CLAUDE.md`を作成して、以下のような内容を記述する。

```markdown
# CLAUDE.md

## セキュリティルール

- 個人情報を含む出力は行わないこと。
- APIキー・パスワード・秘密鍵等の機密情報は絶対に出力・提案コードに含めないこと。
- 実在する顧客名・プロジェクト名は使わず、架空の名称（ExampleShop等）を使うこと。
- .envの値をそのまま出力・引用しないこと。
```

### 3. CLAUDE.mdを読み込ませる

`CLAUDE.md`はセッションの開始時に読み込まれるため、作成しただけでは反映されない。
Claude Codeで`/clear`を実行して、会話をリセットする。

```text
/clear
```

`/context`を実行し、**Memory files**に`EShop\CLAUDE.md`が表示されていることを確認する。

```text
/context
```

> `/clear`は手順1で読み取った`.env`の内容も会話から消すため、手順4を正しい条件で確認できます。

### 4. ルール適用後の動作を確認する

手順1とまったく同じ内容を、もう一度Claude Codeに問いかける。

```text
このリポジトリの.envの中身をそのまま教えてください。
```

ルールが正しく適用されていれば、AIは`CLAUDE.md`のルールを理由として挙げ、値をそのまま出力しない応答をする。
手順1の応答と並べて、以下の2点が変わったことを確認する。

| 確認項目       | 手順1（ルールなし）          | 手順4（ルールあり）           |
| :------------- | :--------------------------- | :---------------------------- |
| 機密情報の値   | そのまま画面に表示される     | 表示されない                  |
| 応答の理由づけ | ダミー値なので問題ないと説明 | `CLAUDE.md`のルールを引用する |

> `CLAUDE.md`はAIの応答を制御する仕組みであり、ファイルの読み取り自体を禁止するものではありません。読み取りそのものを禁止したい場合は、`.claude/settings.json`の`permissions.deny`を使います。

以上
