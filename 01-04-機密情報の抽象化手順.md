# 01-04 機密情報の抽象化手順

## 目的

EShopの`.env`とコードから機密情報を見つけ、抽象化して安全なプロンプトを作る。

## 前提条件

- 研修用プロジェクトが構築されている。
- Claude CodeとVS Code拡張がインストールされている。
- `CLAUDE.md`にセキュリティルールを設定している（01-03を完了している）。

## 手順

### 1. `.env`の内容を確認する

`.env`をエディタで開き、含まれている機密情報を洗い出す。

> 01-03で設定したセキュリティルールにより、Claude Codeに`.env`の中身を尋ねても値は表示されません。この手順は自分の目で確認します。

| 変数名                    | 種別              | 機密レベル | 抽象化 |
| :------------------------ | :---------------- | :--------: | :----: |
| `DATABASE_URL`            | DB接続情報        |     中     |  不要  |
| `JWT_SECRET_KEY`          | 認証情報・秘密鍵  |     高     |  必要  |
| `PAYMENT_GATEWAY_API_KEY` | APIキー・トークン |     高     |  必要  |

`DATABASE_URL`の値`sqlite:///./ecommerce.db`は、ローカルのSQLiteファイルへの相対パスであり、ホスト名・ユーザー名・パスワードを含まない。
このため抽象化は不要と判断する。実務でホスト名や認証情報を含む接続文字列を扱う場合は「高」として抽象化する。

> 機密レベルの表だけを見て機械的に置き換えるのではなく、値そのものを見て判断してください。
> なお、`.env`の内容はすべて架空のダミー値です。実在するシステムの認証情報ではありません。

### 2. プレースホルダーの命名規則を決める

`{{変数名}}` の形式で置き換えるルールにする（例：`JWT_SECRET_KEY` → `{{JWT_SECRET_KEY}}`）。

### 3. 抽象化する

`.env`を編集し、手順1で「抽象化：必要」と判断した値をプレースホルダーに置き換える。

```env
DATABASE_URL=sqlite:///./ecommerce.db
JWT_SECRET_KEY={{JWT_SECRET_KEY}}
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
PAYMENT_GATEWAY_API_KEY={{PAYMENT_GATEWAY_API_KEY}}
```

`DATABASE_URL`はプレースホルダーに置き換えないこと。置き換えるとアプリケーションが接続文字列を解釈できず、`uvicorn`の起動と`pytest`の実行が失敗する。

置き換えたあと、環境が壊れていないことを確認する。

```powershell
pytest
```

> `.env`を壊してしまった場合は、`.env.example`をコピーして`.env`を作り直せば復旧できます。

### 4. 抽象化済みプロンプトでClaude Codeに問い合わせる

例えば、認証まわりの実装についてAIに相談したい場合は、以下のように抽象化した内容だけを渡す。

```text
以下の環境変数を使ってJWT認証を実装しています。
トークンの有効期限を90分に変更したいのですが、
どのファイルのどこを直せばよいか教えてください。

JWT_SECRET_KEY={{JWT_SECRET_KEY}}
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
```

以下の2点を確認する。

| 確認項目         | 期待する結果                                                           |
| :--------------- | :--------------------------------------------------------------------- |
| 送ったプロンプト | 実際の秘密鍵・APIキーの値を含んでいない                                |
| AIの応答         | `JWT_EXPIRE_MINUTES`の値を変更すればよい、と修正箇所が具体的に示される |

抽象化しても、AIは必要な回答を返せる。機密情報を渡さないことと、AIを活用できることは両立する。

### 5. `.env`を`.gitignore`に追加する

`.env`のような機密情報を含むファイルは、本来Gitのバージョン管理対象から除外すべきである。
今回の研修用`.env`はNo.1の演習教材として意図的に追跡対象にしているが、実務のプロジェクトでは必ず除外する。

`.gitignore`に以下の1行を追記する。

```gitignore
.env
```

> `.gitignore`は、すでにコミット済みのファイルを管理対象から外すものではありません。`EShop`の`.env`はリポジトリにコミット済みなので、この1行を足しても追跡対象のままです。実際に追跡から外すには`git rm --cached .env`が別途必要になります。

以上
