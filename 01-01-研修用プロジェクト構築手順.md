# 01-01 研修用プロジェクト構築手順

## 目的

演習で使用する**ECサイトのEShop**をGitHubからクローンし、ローカルで起動できる状態にする。

## 前提条件

- VS Code（Visual Studio Code）がインストールされている。
- Pythonがインストールされている。
- Gitがインストールされている。

## 手順

### 1. リポジトリのクローン

- VS Codeを起動する。
- VS Codeのターミナルを開く。
- 任意の作業フォルダで研修用リポジトリをクローンする。
  ```powershell
  git clone https://github.com/SEC-Online-Boot-Camp/EShop.git
  ```
- プロキシ環境でクローンできない場合は、手順2のプロキシ設定を先に実施し、VS Codeを再起動してからやり直す。
- VS Codeのファイルメニューから「フォルダを開く」を選択し、クローンした`EShop`フォルダを選択する。
- 以降の手順は、すべて`EShop`フォルダを開いた状態で実行する。

### 2. Python環境

- VS Codeのターミナルを開く。
- Pythonのバージョンを確認する（3.11以上であること）。
  ```powershell
  python --version
  ```
- 仮想環境を作成する。
  ```powershell
  python -m venv .venv
  .venv\Scripts\activate
  ```
- 実行許可エラーになる場合は以下のコマンドを実行する。
  ```powershell
  Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
  ```
- ライブラリをインストールする。
  ```powershell
  pip install -r requirements.txt
  ```
- プロキシエラーになる場合は以下のコマンドを実行する（設定後にVS Codeを再起動する）。
  ```powershell
  setx HTTP_PROXY "http://プロキシのアドレス:ポート番号"
  setx HTTPS_PROXY "http://プロキシのアドレス:ポート番号"
  ```

### 3. 初期データの投入

- 研修用プロジェクトの初期データを投入する。
  ```powershell
  python -m app.seed
  ```

### 4. サーバーの起動

- サーバーを起動する。
  ```powershell
  uvicorn app.main:app --reload
  ```
- Swagger UI（`http://127.0.0.1:8000/docs`）で`products`一覧が表示されることを確認する。
- `Ctrl+C`でサーバーを停止する。

### 5. テストの実行

- 単体テストを実行する。
  ```powershell
  pytest
  ```

以上
