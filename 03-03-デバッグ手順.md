# 03-03 デバッグ手順

## 目的

失敗したテストの原因をAIと対話しながら特定・修正し、その過程をデバッグレポートとして記録する。

## 前提条件

- 03-02でテストコード（`tests/test_coupon.py`）を作成し、`ERROR`なく実行できる状態になっている（`FAILED`はあってよい）。
- 02-02で作成したAPI設計書（`docs/クーポンAPI設計書.md`）を参照できる。

`No3`ブランチのクーポン実装には**意図的な誤りが含まれている**。ここからは、
自分のテストが検出した誤りを実装側で直していく。

> **原則、修正するのはテストではなく実装である。** テストが赤いときに、テストの期待値を実装に
> 合わせて書き換えれば緑にはなるが、それでは不具合が残ったままになる。
> 「設計書ではどうなっているか」を必ず確認してから、どちらが間違っているかを判断すること。
>
> **ただし例外がある。** 自分の設計書にあって配布物に実装されていない仕様が原因の失敗は、
> 実装を作り足すのではなく**観点とテストを配布物の仕様に合わせて直す**。03-01の手順2で
> 「配布物の仕様を基準とする」と決めているためである。判断の基準は手順1を参照。

## 手順

### 1. AIにテストを実行させ、失敗を原因ごとにまとめさせる

実行もAIに任せる。Claude Codeはコマンドを実行して結果を読める。

ただし**仮想環境のパスを明示すること**。Claude Codeはコマンドごとに新しいシェルを起動するため、
自分のターミナルで`activate`した仮想環境は引き継がれない。素の`pytest`と書くと
`ModuleNotFoundError: No module named 'fastapi'`になる。

```text
.venv\Scripts\pytest.exe --tb=no -q -rf --disable-warnings を実行して、
失敗しているテストを原因ごとにグループ化してください。まだ修正はしないでください。

グループごとに、次を表にしてください。
- 失敗しているテスト名
- 想定される原因（1行）
- 設計書のどの記述に反しているか。設計書に記載がない場合は「記載なし」と書く

【設計書】
@docs/クーポンAPI設計書.md
```

コマンドの実行にも承認を求められる。読み取りしかしないコマンドなので承認してよい。

**件数のまま1件ずつ潰さない。** 失敗は同じ原因でまとまっていることが多く、1箇所直すと複数が
同時に緑になる。**いくつの論点があるか**を先に押さえること。
10件を超える失敗でも、原因は数個に収まるのが普通である。

グループ化の結果が出たら、それぞれが**実装の欠陥**か**設計書と配布物の仕様差**かを
**自分で**見分ける。扱い方が正反対になるので、修正に入る前に必ず判断する。

| 症状                                               | 判断                 | 対応                                                                                   |
| :------------------------------------------------- | :------------------- | :------------------------------------------------------------------------------------- |
| 設計書に書かれたとおりに動いていない               | 実装の欠陥           | 実装を直す                                                                             |
| 設計書にはあるが、配布物がその仕様で作られていない | 仕様差               | テストを配布物の仕様に合わせて直し、差分をレポートに記録する                           |
| 設計書に記載がなく、03-01で暫定値を置いた観点      | 暫定判断の当たり外れ | 実装の実挙動を仕様として確定させ、テストを直す                                         |
| 設計書自身が「未決」「要確認」と明記している       | 仕様が未確定         | 発注元（講座では講師）に確認して仕様を確定させ、確定した仕様に沿って実装かテストを直す |

> **この判断はAIに任せない。** 任せると「設計書に書いてあるから実装を直す」と一律に判断し、
> **配布物に無い機能を作る作業**が始まってしまう。「設計書のどこに書いてあるか」の
> 列挙まではAIにやらせてよいが、実装を直すのか観点を直すのかは自分で決めること。
>
> 1行目と2行目の境目は迷いやすい。**03-01の手順2で「配布物の仕様を基準とする」と決めている**
> ことを判断の軸にする。配布物がその仕様で作られていないなら、たとえ数行で実装できることでも
> 作り足さない。今回の課題は試験であって、機能の追加実装ではない。

### 2. AIに原因の仮説を説明させる（修正はさせない）

グループを1つ選び、原因を詳しく説明させる。手順1でAIがテストを実行しているので、
pytestの出力を貼り付ける必要はない。テストコードと実装の場所もAIは把握している。

一方、**設計書は毎回渡す**。AIは会話の前の方で出した条件を保持し続けるとは限らず、
設計書が抜けると実装の挙動を正として「テストの期待値のほうが間違っている」と
結論しがちである。

```text
グループ1について、なぜその挙動になるのかを、
設計書の記述と対応づけて説明してください。
まだ修正はしないでください。

【設計書】
@docs/クーポンAPI設計書.md
```

> **いきなり「直して」と言わない。** AIは修正を求められると、テスト側の期待値を実装に
> 合わせて書き換えて辻褄を合わせることがある。先に原因を説明させ、その説明が設計書と
> 整合しているかを自分で確かめてから修正に進む。説明できない修正は採用しない。

### 3. 承認してから、AIに修正と再実行をさせる

説明が設計書と整合していたら、修正を任せる。テストの実行までAIにやらせてよい。

```text
その方針で実装を修正し、.venv\Scripts\pytest.exe を実行して結果を報告してください。
テストコードは変更しないでください。
```

修正の適用時にClaude Codeは承認を求める。**差分を見てから承認する**こと。
`tests/`配下への書き込みを提案してきたら承認しない。

1件直すと別のテストが新しく失敗することがある。**同じ考え方の誤りが複数箇所にある**ケースである。
その場合は続けて対処させる。

```text
いま修正したのと同じ原因が、他の箇所にも潜んでいないか確認してください。
```

> 値が「未設定（NULL / None）」のときの扱いは、判定する場所ごとに書かれるため、
> 同じ見落としが複数箇所に散らばりやすい。SQLでは`x < NULL`が真にならないなど、
> Python側とは挙動が異なる点にも注意する。
>
> 手順1で**仕様差**・**暫定外れ**に分類したグループだけは、実装ではなくテストを直す。
> このときは「テストコードは変更しないでください」を外し、「配布物の仕様に合わせて
> テストの期待値を直してください」と指示する。変更した理由はデバッグレポートに残す。

### 4. AIが直したものを自分で検証する

AIが「全件PASSしました」と報告しても、そのまま信じない。**何を変えたかを自分で見る。**

```powershell
git diff -- app tests
pytest
```

> `git diff`を引数なしで実行すると、No.1で自分が変更した`.env`・`.gitignore`まで差分に出る。
> ここで見たいのはAIが触ったコードなので、`-- app tests`で対象を絞る。

| 確認項目           | 見るポイント                                                                                |
| :----------------- | :------------------------------------------------------------------------------------------ |
| 変更されたファイル | 実装（`app/`配下）を直しているか。`tests/`配下が書き換わっていないか                        |
| 変更の量           | 1行の条件修正で済むはずのものが、大きな書き換えになっていないか                             |
| 仕様差の扱い       | 配布物にない機能（併用チェックなど）を新しく実装していないか                                |
| テストの結果       | 引数なしの`pytest`で、既存55件を含む全件がPASSするか                                        |
| テスト名           | 期待値を変えたテストの名前が実態と合っているか（`_returns_409`のまま200を検証していないか） |

> **`git diff`が最重要である。** テストを緑にする方法はいくらでもあり、AIは求められれば
> 期待値を書き換えることも、条件分岐を足して回避することもできる。
> 「緑になった」ではなく「**設計書どおりに直った**」かどうかを差分で確かめること。

### 5. デバッグレポートを作成する

修正した項目ごとに、以下の形式で記録する。原因の仮説は「〜のバグ」ではなく、
**なぜその挙動になるのか**が読んで分かるように書くこと。

|  #  | エラー内容 | 原因の仮説 | 判断（実装の欠陥／仕様差／暫定外れ／仕様未確定） | 修正内容 |
| :-: | :--------- | :--------- | :----------------------------------------------- | :------- |
|  1  |            |            |                                                  |          |
|  2  |            |            |                                                  |          |

`docs/デバッグレポート.md`として保存する。

## 完了条件

- 引数なしの`pytest`が全件PASSする（既存55件＋自分のクーポンテスト）
- 実装の欠陥は実装側を修正して直している（テストの期待値を実装に合わせて緩めていない）
- 仕様差・暫定外れはテスト側を直し、その判断理由をデバッグレポートに残している
- 設計書が未決としている論点は、確定させた仕様と決めた理由をデバッグレポートに残している
- エラー・原因仮説・修正内容を記録したデバッグレポートが`docs/デバッグレポート.md`として保存されている

## 補足：コードの脆弱性を自動チェックする

デバッグで直した個々の不具合とは別に、Claude Codeの`/security-review`コマンドで
コード全体を脆弱性の観点からレビューできる。時間が余ったら試してみること。

以上
