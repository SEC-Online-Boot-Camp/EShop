# 03-02 テストコード生成手順

## 目的

試験観点シートをもとに、pytestを使ったテストコードを生成する。

## 前提条件

- 03-01で試験観点シート（`docs/試験観点シート.md`）を作成している。
- `No3`ブランチに切り替えた`EShop`をVS Codeで開き、`pytest`が実行できる状態になっている（No.1で作った仮想環境を有効化済み）。
- 03-01の手順0-2で`ecommerce.db`を作り直している。

## 手順

### 1. 既存のテスト構成とフィクスチャを確認する

`tests/conftest.py`を開き、用意されているフィクスチャを確認する。基本的なデータは揃っているので、
ユーザーや商品を自分で作り込む必要はない。

| フィクスチャ                   | 内容                                                     |
| :----------------------------- | :------------------------------------------------------- |
| `client`                       | FastAPIのTestClient                                      |
| `auth_headers`                 | ログイン済みユーザーの認証ヘッダ                         |
| `db_session`                   | テスト用DBセッション（DBの値を直接確認したいときに使う） |
| `sample_product`               | 単価1,000円・`is_sale=False`の商品                       |
| `cart_with_items`              | `sample_product`を4点入れた小計4,000円のカート           |
| `sale_product`                 | 単価2,000円・`is_sale=True`の商品                        |
| `sample_coupon`                | 定率10%・割引上限1,000円・最低購入金額3,000円            |
| `fixed_coupon`                 | 定額500円引き・最低購入金額3,000円                       |
| `unlimited_coupon`             | 定率5%・発行数上限なし                                   |
| `exhausted_coupon`             | 発行数上限に到達済み                                     |
| `expired_coupon`               | 有効期限切れ                                             |
| `make_product` / `make_coupon` | 任意の商品・クーポンを作るファクトリ                     |

> **`make_product`・`make_coupon`が重要である。** 既製のフィクスチャは金額が割り切れる値に
> なっているため、端数のような境界値は自分でデータを作らないと再現できない。
> 例：`make_product(price=1235)`を3点入れると小計3,705円になり、15%の割引で端数が発生する。
>
> ただし`make_coupon`の有効期間は**日数単位**（`valid_from_days`・`valid_to_days`）でしか
> 指定できない。「`valid_to`ちょうど」「終了1秒後」のような秒単位の境界は、
> `valid_to_days=0`では作れず（生成時刻とアサーション時刻の差だけ過ぎた状態になる）、
> ケースが縮退する。厳密に確認したい場合は`db_session`で`valid_to`を直接書き換えること。
> 試験観点シートに秒単位の境界がある場合は、この制約を踏まえて観点を読み替える。

まず既存テストが通ることを確認しておく。これが回帰試験の基準線になる。

```powershell
pytest
```

55件PASSすれば正常である。

### 2. AIにテストコードを生成させ、ファイルに保存させる

試験観点シートと、使えるフィクスチャの一覧をあわせて渡す。フィクスチャを伝えないと、
AIはDBに直接データを作る冗長なテストを書いてしまう。

出力先もプロンプトで指定する。Claude Codeはファイルを直接作成できるので、
生成されたコードを画面からコピーして貼り付ける必要はない。

> ここでも**実装コードは読ませない**。実装を見せると、AIは実装の挙動に合わせたアサーションを書くため、
> 意図的な誤りを含んだ実装に対してもテストが全件PASSしてしまい、次の03-03で直すものがなくなる。
> 根拠にしてよいのは試験観点シートと`tests/conftest.py`だけである。

```text
あなたはPythonのバックエンド開発者です。
以下の試験観点をもとに、FastAPI + pytest のテストコードを作成してください。

【試験観点】
@docs/試験観点シート.md

【利用できるフィクスチャ（@tests/conftest.py に定義済み）】
client, auth_headers, db_session, sample_product, cart_with_items, sale_product,
sample_coupon, fixed_coupon, unlimited_coupon, exhausted_coupon, expired_coupon,
make_product(price=..., name=..., category=..., is_sale=...), make_coupon(code=..., type=..., value=...,
max_discount_amount=..., min_purchase_amount=..., excluded_categories=..., usage_limit=...,
used_count=..., valid_from_days=..., valid_to_days=...)

【条件】
- ロジック（app/coupon.py・app/routers/配下）は読まず、試験観点とフィクスチャを根拠にすること
- ステータスコードだけでなく、レスポンスボディの金額（discount_amount, total）も検証すること
- 端数や境界値のテストは make_product / make_coupon でデータを組み立てること
- テスト同士が実行順に依存しないこと
- 期待結果が「手動確認」の観点はテストコードにしないこと
- 期待値に「暫定」と書かれている観点は、テスト関数にコメントで「暫定」と根拠を残すこと

【出力先】
tests/test_coupon.py として保存してください
```

> ファイルの作成には**承認を求められる**（01-00で確認したClaude Codeの権限モデル）。
> 提案された内容を確認してから承認すること。既存のテストファイルを上書きしようとしていないかも
> あわせて見る。`tests/test_coupon.py`以外への書き込みを提案してきたら承認しない。

### 3. 実行して、失敗を確認する

```powershell
pytest tests/test_coupon.py -v --disable-warnings
```

**この時点では失敗して構わない。** 配布物には意図的な誤りが含まれているため、
観点が正しく書けているほど失敗が出る。失敗が出たら、それは次の03-03で扱う材料になる。

ただし、**`FAILED`（実装が疑わしい）と`ERROR`（テストコードが壊れている）は区別すること。**

| 症状                                                   | 意味                                 | 対処                                      |
| :----------------------------------------------------- | :----------------------------------- | :---------------------------------------- |
| `FAILED ... assert ○○ == △△`                           | テストは動いていて、実装が疑わしい   | そのまま03-03へ                           |
| `ERROR at setup of ...` / `fixture 'xxx' not found`    | 存在しないフィクスチャ名を使っている | ここで直す。手順1の表と名前を突き合わせる |
| `ERROR collecting ...` / `SyntaxError` / `ImportError` | テストファイル自体が読み込めていない | ここで直す（AIにエラー全文を渡して修正）  |

フィクスチャ名を間違えた場合、pytestは`available fixtures:`として**使える名前を全部並べて**教えてくれる。
自分で探す前にこの一覧を見ること。

```none
E       fixture 'cart_with_coupon_items' not found
>       available fixtures: ..., auth_headers, cart_with_items, client, db_session,
        exhausted_coupon, expired_coupon, fixed_coupon, make_coupon, make_product, ...
```

> **`--collect-only`だけでは不十分である。** 構文エラーとimportエラーは収集の段階で止まるが、
> **フィクスチャ名の誤りは収集を素通りする**（収集は成功し、実行して初めて`ERROR at setup`になる）。
> 「収集が通った＝テストが動く」ではないので、必ず実際に実行して確認すること。

```powershell
pytest tests/test_coupon.py --collect-only
```

### 4. レビュー観点で確認する

- 試験観点シートの全項目がカバーされているか（特に回帰の4項目）
- assertの内容は適切か（**ステータスコードだけでなく金額まで検証しているか**）
- 端数・境界値のテストで、実際にその条件が成立するデータになっているか
- **1つのテストが複数の境界を同時に踏んでいないか**（下記参照）
- 「暫定」の期待値にコメントが残っているか（03-03で切り分けに使う）
- 「手動確認」の観点がテスト化されていないか
- テスト間の依存関係はないか（単体で実行しても通るか）

> 2つ目が最重要である。金額が違うだけの不具合はステータスコード200のまま返ってくるため、
> `assert response.status_code == 200`だけのテストでは**絶対に検出できない**。
>
> 3つ目も見落としやすい。たとえば「対象外カテゴリが除外されるか」を確認したいテストで、
> 割引対象金額がたまたま最低購入金額とちょうど同額になっていると、最低購入金額側の判定で
> 先に弾かれ、除外ロジックを観測できない。**確認したい条件以外は境界から離した値にする**のが原則である。
> 失敗したテストの原因が1つに絞れないと、03-03のデバッグで切り分けができなくなる。

## 完了条件

- `tests/test_coupon.py`を実行して`ERROR`が出ない（すべてのテストがPASSまたはFAILEDとして結果を返す。
  FAILEDはあってよい）
- 03-01の試験観点シートの正常系・異常系・境界値・回帰がテストコードに反映されている
- 各テストがレスポンスボディの金額まで検証している

以上
